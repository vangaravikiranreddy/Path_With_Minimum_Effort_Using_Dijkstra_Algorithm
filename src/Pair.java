class Pair {
    int r;   // row
    int c;   // column
    int diff;  // difference
    public Pair(int r, int c, int diff) {
        this.r = r;
        this.c = c;
        this.diff = diff;
    }
}